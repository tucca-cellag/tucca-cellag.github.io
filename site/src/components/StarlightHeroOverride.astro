---
// Empty override for Starlight's built-in Hero component.
// Setting `hero: {}` in a page's frontmatter makes Starlight enter the hero
// branch (suppressing the auto PageTitle <h1>) without injecting Starlight's own
// hero UI, since the custom landing markup in index.mdx replaces it.
---
